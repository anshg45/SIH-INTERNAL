import React from 'react';
import { BarChart3, AlertTriangle } from 'lucide-react';
import { PHARMACOVIGILANCE_EVENTS } from '../../data/mockData';

export default function AeSummaryView() { const byType = ['SAE', 'AE'].map((type) => ({ type, count: PHARMACOVIGILANCE_EVENTS.filter((event) => event.type === type).length })); return <div className="p-6 space-y-5 max-w-6xl mx-auto"><Header title="Study-level AE Summary Dashboard" subtitle="Aggregate safety trends across active protocols." icon={BarChart3} /><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{byType.map((item) => <div key={item.type} className="bg-white border border-slate-200 rounded-xl p-5"><p className="text-xs uppercase font-bold text-slate-500">{item.type} reports</p><p className="text-3xl font-black text-slate-900 mt-2">{item.count}</p><p className="text-xs text-slate-500 mt-1">Across monitored studies</p></div>)}</div><div className="bg-white border border-slate-200 rounded-2xl p-5"><h2 className="text-sm font-bold text-slate-900 mb-4">Signal overview</h2>{PHARMACOVIGILANCE_EVENTS.map((event) => <div key={event.id} className="flex items-center justify-between border-b border-slate-100 py-3"><span className="text-xs font-mono font-bold">{event.id} · {event.trialId}</span><span className="flex items-center gap-1 text-xs text-amber-700"><AlertTriangle className="w-4 h-4" />{event.status || 'Under review'}</span></div>)}</div></div>; }
function Header({ title, subtitle, icon: Icon }) { return <div className="bg-rose-950 text-white rounded-2xl p-6"><div className="flex items-center gap-3"><Icon className="w-6 h-6 text-rose-300" /><div><h1 className="text-xl font-bold">{title}</h1><p className="text-xs text-rose-100/70 mt-1">{subtitle}</p></div></div></div>; }
